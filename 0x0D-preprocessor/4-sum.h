#ifndef SUM_H
#define SUM_H

/**
 * SUM - macro function like for sum
 *
 * @x: first argument
 * @y: second argument
 * Desc: Header file that defines a function like SUM(x, y)
 *		that computes the sum of two numbers.
 */

#define SUM(x, y) ((x) + (y))

#endif
