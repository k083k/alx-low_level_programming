#include "main.h"

/**
 * _abs - calculates absolute value of input
 * @c: input for processing
 * Return: return the absolute value
 */

int _abs(int c)
{

	if (c < 0)
	{
		c *= -1;
	}
	return (c);
}
