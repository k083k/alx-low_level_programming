#include <stdio.h>

/**
 * main - program that outputs its name followed by new line
 * @argc: parameter/argument count
 * @argv: argument vector
 *
 * Return: ALways 0 (Success)
 */

int main(int argc, char **argv)
{
	argc = argc;
	printf("%s\n", argv[0]);
	return (0);
}
